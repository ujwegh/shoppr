server:
  port: 8102

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    enabled: false

spring:
  application:
    name: products
  cloud:
    config:
      enabled: false

webClient:
  loadBalanced:
    maxConnections: 100
    isKeepAlive: false
  default:
    maxConnections: 100
    isKeepAlive: false

products:
  service:
    url: http://products